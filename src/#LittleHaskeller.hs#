x{-|
Module      : LittleHaskeller
Description : Functional programming exercises in Haskell
Copyright   : (c) 2026
License     : BSD-3-Clause
Maintainer  : example@example.com

This module contains functional programming exercises,
demonstrating recursion and higher-order functions.
-}

module LittleHaskeller
    ( mySum
    , mySumFold
    ) where

-- | Q001: Haskell の sum と同様の機能の関数 mySum を書け。
-- (再帰を用いるパターン, fold を用いるパターン)
--
-- sum :: (Num a) => [a] -> a
-- sum ns
--     数値のリスト ns の総和を返す。
--     see also: product, foldl
--         sum [1, 2, 3]  = 6
--         sum []         = 0

-- | 再帰を用いたバージョン
mySum :: (Num a) => [a] -> a
mySum [] = 0
mySum (x:xs) = x + mySum xs

-- | foldl を用いたバージョン
mySumFold :: (Num a) => [a] -> a
mySumFold = foldl (+) 0
